---
- name: this playbook installs the apache
  hosts: appservers
  become: true
  tasks:
    - name: update apt cache
      apt:
        update_cache: yes
    - name: install package
      apt:
        name: apache2
        state: present
    - name: start the service
      service:
        name: apache2
        state: started